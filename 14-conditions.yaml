- name: "Conditions"
  hosts: local
  connection: local
  tasks:
  - name: "Conditions"
    ansible.builtin.debug:
      msg: "{{ ansible_facts }}"
  - name: "Conditions"
    ansible.builtin.debug:
      msg: "Yes it is Redhat"
    when: ansible_facts.os_family == "RedHat"
    